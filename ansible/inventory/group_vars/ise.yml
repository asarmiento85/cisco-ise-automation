---
# Connection args consumed by cisco.ise.* modules via module_defaults.
ise_connection:
  ise_hostname: "{{ ise_hostname }}"
  ise_username: "{{ ise_username }}"
  ise_password: "{{ ise_password }}"
  ise_verify: false
  ise_wait_on_rate_limit: true
  ise_debug: false
